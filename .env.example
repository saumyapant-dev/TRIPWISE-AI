# TripWise AI - Frontend Environment Configuration Template
# Secret API keys belong strictly in server/.env — NEVER in this file!
# Vite prefixes with VITE_ are inlined into client-side JS bundles.

# Optional: Custom backend API base URL (defaults to '/api')
VITE_API_BASE_URL=
